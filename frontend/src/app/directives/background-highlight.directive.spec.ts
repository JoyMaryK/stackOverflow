import { BackgroundHighlightDirective } from './background-highlight.directive';

describe('BackgroundHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
